# Nginx

Reverse proxy and load-balancer configuration. Nginx balances requests across two Angular SSR Node containers, applies static asset cache headers, and forwards all application routes to SSR.
