# App

Angular standalone application source. The root component owns global outlets and PrimeNG toast; feature routing is defined in `app.routes.ts`, and application providers are registered in `app.config.ts`.
