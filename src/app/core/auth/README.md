# Auth

Typed authentication models, NgRx actions/reducer/effects/selectors, facade, and guard. The implementation uses real API placeholders through `/auth/*` endpoints and stores issued tokens only after a successful backend response.
