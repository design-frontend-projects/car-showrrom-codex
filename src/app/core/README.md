# Core

Cross-cutting application services live here: authentication, API access, interceptors, logging, maps, and onboarding. Core code should be singleton-oriented and imported by providers or feature shells, not by presentation-only utilities.
