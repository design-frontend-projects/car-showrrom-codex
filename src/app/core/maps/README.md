# Maps

Google Maps loading infrastructure. `GoogleMapsLoaderService` loads the Maps JavaScript API only in the browser and reads API settings from the active environment file.

Keep private API keys out of source control. The default empty key renders a safe placeholder in map components.
