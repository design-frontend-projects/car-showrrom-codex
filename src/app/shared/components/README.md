# Components

Standalone shared UI components. Components here should be focused, typed, and reusable across landing, admin, and client feature areas.

- `register-form` emits typed registration requests.
- `google-map-picker` wraps Google Maps with SSR-safe loading, overlays, search, route drawing, and typed outputs.
